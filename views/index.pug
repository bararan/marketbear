html
    head
        meta(charset="utf-8")
        script(src="https://cdnjs.cloudflare.com/ajax/libs/d3/4.9.1/d3.min.js")
        script(src="/socket.io/socket.io.js")
        script(src="https://cdnjs.cloudflare.com/ajax/libs/d3/4.9.1/d3.min.js")
        title MarketBear
        link(
            rel="stylesheet" 
            href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" 
            integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" 
            crossorigin="anonymous"
        )
        link(
            rel="stylesheet" 
            href="../styles/stylesheet.css"
        )
        link(
            href="https://fonts.googleapis.com/css?family=Megrim|Open+Sans"
            rel="stylesheet"
        )
    body
        div(class="container-fluid")
            h2 STOCKS
        div(id="graph-container" class="container-fluid")
        div(id="stocks" class="container-fluid")
            div(id="search-box" class="info-card")
                div(id="instructions" class="container-fluid") SEARCH 
                    a(href="http://eoddata.com/symbols.aspx" target="_blank") TICKER
                form(id="search-ticker" class="input-group" onsubmit="emitTicker(this.ticker.value);return false;")
                    input(type="text" class="form-control" name="ticker" placeholder="E.g: FB" autofocus)
                    span(class="input-group-btn")
                        button(type="submit" class="btn btn-default" value="" formaction="")
                            span(class="glyphicon glyphicon-search")

        script(src="../scripts/bodyScripts.js")