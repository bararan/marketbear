html
    head
        meta(charset="utf-8")
        script(src="https://cdnjs.cloudflare.com/ajax/libs/d3/4.9.1/d3.min.js")
        script(src="/socket.io/socket.io.js")
        script(src="https://cdnjs.cloudflare.com/ajax/libs/d3/4.9.1/d3.min.js")
        title MarketBear
        link(
            rel="stylesheet" 
            href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" 
            integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" 
            crossorigin="anonymous"
        )
        link(
            rel="stylesheet" 
            href="../styles/stylesheet.css"
        )
        link(
            href="https://fonts.googleapis.com/css?family=Megrim|Open+Sans"
            rel="stylesheet"
        )
    body
        div(id="graph-container" class="container-fluid")
            //- svg(id="graph" height="100%" width="100%")
            div(id="tooltip")
        div(id="stocks" class="container-fluid")
        form(id="ticker-search" onsubmit="emitTicker(this.ticker.value);return false;")
            input(type="form-control" name="ticker")
            input(type="submit" class="btn" value="GO!" formaction="")
        script(src="../scripts/bodyScripts.js")